import 'package:flutter/material.dart';

class BarItemsScreen extends StatelessWidget {
  const BarItemsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Bar Item"),
      ),
    );
  }
}
